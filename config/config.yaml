# 应用公共配置（所有环境共享）
app:
  env: "dev"

http:
  addr: ":8080"
  basePath: "/api/v1/general-agent"

log:
  level: "info"
  encoding: "json"

mysql:
  host: "127.0.0.1"
  port: 3306
  username: "root"
  password: ""
  database: "general_agent"
  maxIdleConns: 10
  maxOpenConns: 100
  connMaxLifetime: 3600